/**
 * Base policy to define custom bouncer policies
 */
export declare abstract class BasePolicy {
    static booted: boolean;
    static actionsMetaData: Record<string, {
        allowGuest: boolean;
    }>;
    static boot(): void;
    /**
     * Set metadata for a action name
     */
    static setActionMetaData(actionName: string, options: {
        allowGuest: boolean;
    }): void;
}
