import { AuthorizerResponse, BouncerAuthorizer } from './types.js';
type AuthorizerToAbility<Authorizer> = Authorizer extends (user: infer User, ...args: infer Args) => AuthorizerResponse | Promise<AuthorizerResponse> ? {
    allowGuest: false;
    original: Authorizer;
    execute(user: User | null, ...args: Args): AuthorizerResponse | Promise<AuthorizerResponse>;
} : never;
/**
 * Helper to convert a user defined authorizer function to a bouncer ability
 */
export declare function ability<Authorizer extends BouncerAuthorizer<any>>(options: {
    allowGuest: boolean;
}, authorizer: Authorizer): AuthorizerToAbility<Authorizer>;
export declare function ability<Authorizer extends BouncerAuthorizer<any>>(authorizer: Authorizer): AuthorizerToAbility<Authorizer>;
export {};
