import { Group } from '@japa/runner/core';
import { test } from '@japa/runner';
import { LockStore } from './types/main.js';
import 'typescript-log';
import '../drivers-ClN4eGOp.js';
import 'knex';
import 'kysely';
import 'ioredis';
import '@aws-sdk/client-dynamodb';

declare function registerStoreTestSuite(options: {
    test: typeof test;
    createStore: () => LockStore;
    configureGroup?: (group: Group) => any;
}): void;

export { registerStoreTestSuite };
