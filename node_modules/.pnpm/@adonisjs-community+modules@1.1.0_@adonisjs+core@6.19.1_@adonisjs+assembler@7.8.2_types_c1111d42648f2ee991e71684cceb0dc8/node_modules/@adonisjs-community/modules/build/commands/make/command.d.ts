import MakeCommand from '@adonisjs/core/commands/make/command';

/**
 * Make a new ace command
 */
declare class MMakeCommand extends MakeCommand {
    module: string;
    run(): Promise<void>;
}

export { MMakeCommand as default };
