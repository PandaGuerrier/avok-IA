import MakeService from '@adonisjs/core/commands/make/service';

/**
 * Make a new service class
 */
declare class MMakeService extends MakeService {
    module: string;
    run(): Promise<void>;
}

export { MMakeService as default };
