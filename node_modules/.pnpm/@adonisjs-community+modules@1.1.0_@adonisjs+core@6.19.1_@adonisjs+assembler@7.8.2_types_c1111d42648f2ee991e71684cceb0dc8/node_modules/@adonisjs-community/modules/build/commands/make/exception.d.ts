import MakeException from '@adonisjs/core/commands/make/exception';

/**
 * Make a new exception class
 */
declare class MMakeException extends MakeException {
    module: string;
    run(): Promise<void>;
}

export { MMakeException as default };
