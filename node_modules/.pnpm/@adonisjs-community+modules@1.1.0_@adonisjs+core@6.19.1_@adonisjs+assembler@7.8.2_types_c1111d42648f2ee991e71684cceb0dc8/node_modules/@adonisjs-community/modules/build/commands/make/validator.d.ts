import MakeValidator from '@adonisjs/core/commands/make/validator';

/**
 * Make a new VineJS validator
 */
declare class MMakeValidator extends MakeValidator {
    module: string;
    run(): Promise<void>;
}

export { MMakeValidator as default };
