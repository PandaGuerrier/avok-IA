import MakeEvent from '@adonisjs/core/commands/make/event';

/**
 * The make event command to create a class based event
 */
declare class MMakeEvent extends MakeEvent {
    module: string;
    run(): Promise<void>;
}

export { MMakeEvent as default };
