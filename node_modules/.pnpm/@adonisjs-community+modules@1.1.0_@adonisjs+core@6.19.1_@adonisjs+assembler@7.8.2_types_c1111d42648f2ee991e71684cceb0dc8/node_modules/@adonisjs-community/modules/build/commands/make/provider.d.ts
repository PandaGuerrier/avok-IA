import MakeProvider from '@adonisjs/core/commands/make/provider';

/**
 * Make a new provider class
 */
declare class MMakeProvider extends MakeProvider {
    #private;
    module: string;
    run(): Promise<void>;
}

export { MMakeProvider as default };
