import MakeModel from '@adonisjs/lucid/commands/make_model';

declare class MMakeModel extends MakeModel {
    module: string;
    private runMakeControllerModule;
    private runMakeFactoryModule;
    /**
     * Execute command
     */
    run(): Promise<void>;
}

export { MMakeModel as default };
