import MakeListener from '@adonisjs/core/commands/make/listener';

/**
 * The make listener command to create a class based event
 * listener
 */
declare class MMakeListener extends MakeListener {
    module: string;
    run(): Promise<void>;
}

export { MMakeListener as default };
